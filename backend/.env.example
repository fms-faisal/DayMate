# Backend environment template (copy to `.env` locally and fill in values)
# NOTE: Do NOT commit `.env` with real secrets. Use your hosting provider's env vars for production (Render, Vercel, etc.).

# TomTom (Traffic) API key - used for real-time traffic
TOMTOM_API_KEY=

# News provider API key (NewsAPI or similar)
NEWS_API_KEY=

# Google Gemini / AI provider API key
GEMINI_API_KEY=

# Comma-separated list of allowed origins for CORS (no spaces required)
# Example: http://localhost:5173,https://your-frontend.vercel.app
ALLOWED_ORIGINS=http://localhost:5173

# DO NOT set PORT or HOST here for production deployments on Render/Vercel.
# Platform providers inject a runtime PORT; the application will read $PORT at startup.# DayMate Backend Environment Variables
# Copy this file to .env and fill in your API keys

# WEATHER: Open-Meteo API - NO API KEY NEEDED!
# Completely free, no registration required.
# https://open-meteo.com/

# NewsAPI Key - FREE tier for development
# Sign up at https://newsapi.org/ for a free developer key
NEWS_API_KEY=your_newsapi_key_here

# Google Gemini API Key - FREE tier available
# Get your free key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# TRAFFIC API - TomTom Traffic API (Open Traffic - World Bank)
# Based on OSM and Telenav data - Fully free and open source
# Data quality depends on city - Not real-time everywhere
# Get free API key at https://developer.tomtom.com/
TOMTOM_API_KEY=your_tomtom_api_key_here

# CORS Allowed Origins (comma-separated for multiple)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Server Configuration
PORT=8000
HOST=0.0.0.0
